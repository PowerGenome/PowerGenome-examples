# ENVIRONMENT VARIABLES
# PowerGenome v0.8 reads all settings directly from the YAML files in this folder
# (see data.yml, resources.yml, etc.), so no environment variables are required.
# The keys below are kept for compatibility with older configurations that loaded
# data paths from environment variables.
PUDL_DB:
PG_DB:
EFS_DATA:
DISTRIBUTED_GEN_DATA:
